"""
Orbit — Rotating Social Club (buildable core).

A pure-standard-library Python package implementing the heart of the Orbit
product: the rotation engine (keep-50/swap-50 with anti-clique + constraints),
the domain models, and an in-memory service layer for the core loops.

Run the tests:   python -m unittest discover -s tests -v
Run the demo:    python -m orbit.demo     (from the code/ directory)

No third-party dependencies are required.
"""

__version__ = "0.1.0"
